__pycache__/
.venv/
.venv_new/
.pytest_cache/
.DS_Store

# Secrets — never commit the real key (use .env.example as the template)
.env

# Runtime persistence file (regenerated by the app/demo)
data.json